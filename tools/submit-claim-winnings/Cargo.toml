[package]
name = "submit-claim-winnings"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "submit-claim-winnings"
path = "src/main.rs"

[dependencies]
miden-client-cli = "0.15"
miden-client = { version = "0.15", features = ["std"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
