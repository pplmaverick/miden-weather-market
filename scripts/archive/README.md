# Archive

One-off `miden-client exec` probe/test scripts from reverse-engineering proc hashes and
storage layout on the v3/v4 contract iterations (pre-v7). Not part of the active workflow —
kept for reference only. See `../wait_and_settle.sh` for the current settlement helper.
